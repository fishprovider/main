{
  "name": "@fishprovider/trade",
  "version": "7.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "postinstall": "npm run build -w ../ctrader-api -w ../metatrader-api -w ../meta-api",
    "lint": "eslint --cache --fix .",
    "type-check": "tsc --noEmit",
    "test": "echo trade",
    "clean": "rm -rf dist *.tsbuildinfo && mkdir dist",
    "build": "tsc -p tsconfig.build.json",
    "ci-lint": "eslint --cache --fix",
    "ci-type-check": "tsc-files --noEmit"
  },
  "dependencies": {
    "@fishprovider/ctrader-api": "*",
    "@fishprovider/metatrader-api": "*",
    "@fishprovider/meta-api": "*"
  }
}