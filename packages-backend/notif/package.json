{
  "name": "@fishprovider/notif",
  "version": "7.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "postinstall": "npm run build -w ../slack && npm run build -w ../discord && npm run build -w ../firebase && npm run build -w ../expo",
    "lint": "eslint --cache --fix .",
    "type-check": "tsc --noEmit",
    "test": "echo 'TODO: add tests'",
    "clean": "rm -rf dist *.tsbuildinfo",
    "build": "tsc -p tsconfig.build.json",
    "ci-lint": "eslint --cache --fix",
    "ci-type-check": "tsc-files --noEmit"
  },
  "dependencies": {
    "@fishprovider/discord": "*",
    "@fishprovider/expo": "*",
    "@fishprovider/firebase": "*",
    "@fishprovider/slack": "*"
  }
}