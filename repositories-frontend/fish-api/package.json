{
  "name": "@fishprovider/repository-fish-api",
  "version": "7.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "lint": "eslint --cache --fix .",
    "type-check": "tsc --noEmit",
    "test": "echo repository-fish-api",
    "clean-build": "rm -rf dist && mkdir dist",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@fishprovider/core-new": "*",
    "axios": "^1.5.0"
  }
}