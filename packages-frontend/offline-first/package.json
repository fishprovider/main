{
  "name": "@fishprovider/offline-first",
  "version": "6.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "lint": "eslint --cache --fix .",
    "type-check": "tsc --noEmit",
    "test": "npm run repository-offline-first",
    "clean-build": "rm -rf dist && mkdir dist",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@fishprovider/services": "*",
    "@fishprovider/fish-api": "*",
    "@fishprovider/local": "*",
    "@fishprovider/store": "*"
  }
}