enum ServerCommands {
  status = 'status',
  stop = 'stop',
  resume = 'resume',
  restart = 'restart',
  restartProcess = 'restartProcess',
  destroy = 'destroy',
  kill = 'kill',
}

export default ServerCommands;
